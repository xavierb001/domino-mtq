{
    "name": "domino-app",
    "version": "1.0.0",
    "scripts": {
        "start": "node domino-backend/server.js",
        "build": "cd domino-frontend && npm install && npm run build",
        "serve-heroku": "cd domino-frontend && npm install && npm run build && vite preview --host 0.0.0.0 --port $PORT",
        "heroku-postbuild": "cd domino-frontend && npm install && npm run build"
    },
    "engines": {
        "node": ">=14.0.0"
    },
    "dependencies": {
        "serve": "^14.2.4"
    },
    "devDependencies": {
        "vite": "^6.0.11"
    }
}
